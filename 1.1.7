#Requirement C.1.1.7: Overzicht van afbeeldingen met hun metadata
    if(optie == 8):
        #select alle afbeeldingen met hun metadata
        c.execute("select file_name, file_extract_name, file_extension, file_md5_hash, file_creation_time, file_modification_time, file_acces_time "
            "from Files "
            "where file_extension "
            "LIKE '.jpg' or file_extension like '.jpeg' or file_extension like '.png' or file_extension like '.tiff'"")

        #maak een waarde aan die de informatie uit de SQL querie overneemt.
        querie8 = c.fetchall()
        #opslagfile maken voor compound files
        reportCompound = locatieReports + "Afbeeldingen.csv"

        #maak reportfile aan
        with open(reportCompound, 'w') as csvfile:
            writer = csv.writer(csvfile, delimiter=",")
            i = 0
            #voor elke row uit de select hierboven
            while i < len(querie8):
                #Headers aanmaken voor tabel
                if i == 0:
                    writer.writerow(['File-Name:', 'File-type:'])
                #anderzijds write hij de data naar de file
                print "Compound-file found..."
                writer.writerow(querie8[i])
                #itereren over de rows uit de database
                i = i + 1
        print "Er is een rapport gegenereerd met daarin alle metadata van de afbeeldingen op deze image"
        print "Deze is te vinden in: "+ reportCompound
        print "De bestanden in dit rapport kunt u terugvinden in: " + locatieExtracts
        print "U wordt automatisch teruggestuurd naar het werkmenu..."
        time.sleep(10)
        werkMenu(casusNaam, connectie, c, imageBestand)

    if(optie == 100):
        print "Het programma wordt over 3 seconden afgesloten..."
        connectie.close()
        time.sleep(3)
        sys.exit()



